"""A browser console for demonstrating Ledger.

Everything demo-only lives under this package. ``src/`` is untouched — the
portal composes the product's own application factory and adds routers beside
it. See ``demo/portal/app.py`` for how.
"""
